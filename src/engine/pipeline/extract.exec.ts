import type { FaultlineConfig } from '../../types.js'

///////////////////////////////////////////////////////////////////////// API //

/**
 * Execute Extract
 *
 * Stub for the extraction phase. Implemented in a later phase.
 *
 * @param _config - The resolved faultline configuration.
 */
export const execute_extract = async (_config: FaultlineConfig): Promise<void> => {
  throw new Error('Extract phase not yet implemented')
}
