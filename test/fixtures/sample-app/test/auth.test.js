const { register: _register, login: _login } = require('../src/services/auth')

describe('Auth Service', () => {
  test('register creates a new user', async () => {
    // test stub
  })

  test('login returns a JWT token', async () => {
    // test stub
  })

  test('login rejects invalid credentials', async () => {
    // test stub
  })
})
